pipeline {
    agent { label 'ansible-node' }

    environment {
        AWS_SHARED_CREDENTIALS_FILE = '/home/ec2-user/.aws/credentials'
    }

    stages {
        stage('Run Ansible Playbook') {
            steps {
                dir("${WORKSPACE}") {
                    sh 'ansible-playbook Ansible-PlayBooks/EC2-Playbook.yaml'
                }
            }
        }
    }
}
